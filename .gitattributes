# Keep LF in the working tree on every platform: Prettier is configured with
# endOfLine=lf, so a CRLF checkout on Windows would fail the format check.
* text=auto eol=lf

# Binary assets must never be normalised.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
