/**
 * Demo configuration.
 *
 * Placeholder values until there is a real domain and a published app. Both
 * are needed before the first pilot, not before the first meeting.
 */

/** Where a shared link lands for someone without the app installed. */
export const PUBLIC_SITE_URL = 'https://agora.example';

/** URL scheme registered in app.json, used for deep links. */
export const APP_SCHEME = 'agora';
