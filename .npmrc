# React Native / Metro does not resolve pnpm's symlinked node_modules reliably.
# A hoisted layout keeps the Expo app working inside the workspace.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
