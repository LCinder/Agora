/**
 * Shared domain model, validation schemas and pure business logic.
 *
 * Everything in this package must stay framework-free: no React, no Expo,
 * no Next.js. Both the mobile app and the web panel depend on it.
 */

export const CORE_PACKAGE_NAME = '@agora/core';
